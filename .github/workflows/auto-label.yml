name: Auto Label

on:
    pull_request:
        types: [opened, synchronize]

jobs:
    label:
        name: Auto Label PR
        runs-on: ubuntu-latest
        timeout-minutes: 5

        steps:
            - name: Label PR
              uses: actions/labeler@v5
              with:
                  sync-labels: true
